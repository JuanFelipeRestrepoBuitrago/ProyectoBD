Requeriments for MySQL project:

py -m pip install --upgrade pip
pip install django
pip install django-bootstrap-v5
pip install mysqlclient

Requeriments for MongoDB project:

py -m pip install --upgrade pip
pip install django
pip install mongoengine
pip install django-bootstrap-v5